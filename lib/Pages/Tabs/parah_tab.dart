
import 'package:flutter/cupertino.dart';

class ParahTab extends StatelessWidget {
  const ParahTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Parah"),
    );
  }
}
